#include "main.h"

/**
 * mul - Multiplies two integers.
 * @a: The first number to multiply
 * @b: The second number to multiply with the first number
 * Return: The result of multiplication betweem a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
