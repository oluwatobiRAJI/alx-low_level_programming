#include "main.h"

/**
 * _abs - Computes the absolte value of an integer
 * @n: int to check
 *
 * Return: The absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
